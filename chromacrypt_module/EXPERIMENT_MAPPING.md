# ChromaCrypt Module Taxonomy and Legacy Pipeline Transference Mapping

This document provides a rigorous architectural ledger mapping the final execution modules contained within the unified `chromacrypt_module` environment to their direct decentralized counterparts found within the legacy `working_scripts` archive. 

This matrix formalizes the empirical transition, explicitly outlining continuous function maintenance, parameter standardizations, and optimizations executed directly over the sub-graphs.

---

## 1. Discriminative Analysis Execution Sub-Graphs

The discriminative execution directory isolates localized evaluation routines computing perceptual boundary vulnerabilities across Cartesian and Cylindrical spaces.

### Cross-Model Transferability Intersections
* **Core Module:** `benchmark_cross_model_channels.py`
* **Legacy Mappings:** 
  * `working_scripts/Benchmarks/benchmark_cross_model_channels.py`
  * `working_scripts/Experiments/experiment_natural_cross_model.py`
* **Mechanisms:** Fuses un-targeted natural validation frameworks into a single unified script. Replaces statically configured model loading logic with dynamic iterative array resolution defined strictly via the unified configuration schema.

### Defensive Algorithm Matrices
* **Core Module:** `benchmark_defenses.py`
* **Legacy Mappings:** 
  * `working_scripts/Benchmarks/benchmark_defenses.py`
* **Mechanisms:** Maintains analytical evaluation algorithms executing canonical adversarial defenses against evaluated coordinate shifts. Standardized optimization tracking arrays prevent unmanaged memory saturation.

### Exhaustive Suite Iterations
* **Core Module:** `benchmark_full_suite.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_full_benchmark.py`
* **Mechanisms:** Systematically aggregates empirical constraints mapping overall validation configurations to single-call outputs for peer-reviewed Table formatting.

### Boundary Truncation Operations (Gamut Mapping)
* **Core Module:** `benchmark_gamut_ablation.py`
* **Legacy Mappings:** 
  * `working_scripts/Benchmarks/benchmark_gamut_ablation.py`
  * `working_scripts/Experiments/experiment_gamut_ablation.py`
* **Mechanisms:** Centralizes variable definitions controlling $K=12$ iterative sequence mapping implementations. Isolates and tests execution cycle duration bounds against strict straight-through-estimator controls.

### Structural Degradation Sweeps
* **Core Module:** `benchmark_lpips_sweep.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_lpips_threshold_sweep.py`
* **Mechanisms:** Preserves linear scaling validation sequences tracking spatial metric anomalies. Overrides hard-coded looping architectures with structured generator iterators to isolate tensor dependencies natively.

### Architectural Performance Metrics
* **Core Module:** `benchmark_overhead.py`
* **Legacy Mappings:** 
  * `working_scripts/Benchmarks/benchmark_overhead.py`
* **Mechanisms:** Computes and standardizes computational complexity multipliers accurately, verifying exact ms/iter latencies by rigorously tracking raw parameter mapping executions without extraneous memory logging blocks.

### Null-Hypothesis Statistical Isolation
* **Core Module:** `benchmark_random_noise_baserate.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_random_noise_baserate.py`
* **Mechanisms:** Maps uniform Gaussian noise structures utilizing statically seeded array generations. Preserves exactly the required parameter injection metrics required to baseline continuous vulnerability assertions.

### Isolated SOTA Matrix Evaluations
* **Core Module:** `benchmark_robust_models.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_robust_sota.py`
* **Mechanisms:** Restricts architectural validations entirely to models verified externally as state-of-the-art for $L_\infty$ optimization resistance.

### Parameterized AutoAttack Framework Integrations
* **Core Module:** `benchmark_sota_autoattack.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_sota_autoattack_comparison.py`
* **Mechanisms:** Implements critical parameter overrides enabling the continuous mapping of AutoAttack bounding criteria onto cyclic constraint geometries. Explicitly incorporates epsilon remapping constants ($eps\_rgb = 0.01568$) required dynamically within cylindrical initialization paths and maintains untargeted relative labels `$preds\_clean$` validation metrics for parity consistency. Dynamic batch dimension compression ($N \leq 4$) natively prevents VRAM exhaustion algorithms without altering sub-gradient evaluations.

### Canonical Geometric Vulnerability Validations
* **Core Module:** `benchmark_structural_mechanisms.py`
* **Legacy Mappings:** 
  * `working_scripts/Benchmarks/benchmark_chroma_attack.py`
* **Mechanisms:** Establishes and codifies definitive proof structures verifying native separation of intensity vectors scaling entirely across perceptual components identically. Assures empirical replication paths for formal visual artifact generation matrices.

### General Adversarial Portability Analyses
* **Core Module:** `benchmark_transferability.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_transferability.py`
  * `working_scripts/Benchmarks/benchmark_transferability_ablation.py`
* **Mechanisms:** Compiles dual-methodological boundaries verifying that spatial optimization configurations generated against local proxy models natively map directly across independently structured classifier targets.

---

## 2. Generative Purification Sub-Graphs

The generative testing architectures evaluate spatial cleansing behaviors natively utilizing deterministic noise-induction schema.

### Conditional State Modeling (DiffPure)
* **Core Module:** `benchmark_diffpure.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_diffusion_purification.py`
* **Mechanisms:** Validates score-based structural regeneration sequences against adversarial constraints natively over established parameter diffusion constants.

### Advanced Autoregressive Denoising (SD 3.5)
* **Core Module:** `benchmark_sd35_purification.py`
* **Legacy Mappings:** 
  * `working_scripts/Experiments/experiment_sd35_purification.py`
* **Mechanisms:** Maps continuous image reconstruction behaviors directly analyzing stability variance metrics utilizing standard pre-trained generative arrays.

---

## 3. Core Standardization Enhancements

The architectural migration from `working_scripts` to the formalized `chromacrypt_module` explicitly implements absolute parity optimizations tracking systematic execution stability.

* **Unified Configuration Initialization (`chromacrypt_config.json`):** Formally isolates execution variables across all experimental matrices. Completely eliminates unstructured variable overrides documented historically within raw mathematical execution definitions.
* **Deterministic VRAM Culling Algorithms:** Mandates batch size matrix isolation thresholds during complex optimization sequences to guarantee consumer hardware memory compliance seamlessly avoiding CUDA allocation lockups.
* **Array Normalization Enforcement:** Standardizes normalization wrappers wrapping external SOTA parameters natively precluding spatial bound evaluations mapping directly against raw unstructured target structures.
